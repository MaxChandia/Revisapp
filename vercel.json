{
  "version": 2,
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/Revisapp/browser"
      }
    }
  ],
  "scripts": {
    "build": "npm run vercel-build"
  }
}
